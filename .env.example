# GitHub OAuth Application
# Create one at: https://github.com/settings/developers (OAuth Apps tab)
GITHUB_ID=your_github_client_id
GITHUB_SECRET=your_github_client_secret

# NextAuth Configuration
# For local development: http://localhost:3000
# For production (Vercel): https://git-unwrapped.vercel.app
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_with_openssl_rand_base64_32

# OAuth Callback URLs (for GitHub App configuration):
# Local: http://localhost:3000/api/auth/callback/github
# Production: https://git-unwrapped.vercel.app/api/auth/callback/github

# Database
# For local development (SQLite):
DATABASE_URL="file:./dev.db"

# For production (Vercel with PostgreSQL - recommended):
# 1. Add a Postgres database in Vercel (or use Neon, Supabase, etc.)
# 2. Vercel will auto-populate POSTGRES_PRISMA_URL or DATABASE_URL
# 3. Or manually set: DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Admin Configuration
# Set your email here to grant admin access
ADMIN_EMAIL="your_email@example.com"
